# AI駆動開発実装ガイド

AI駆動開発をPJに導入するにあたって、下記の順で始めることができます。
詳細は各パートで説明します。

1. 概要
1. 前提
1. 実装
    1. 環境構築
    1. 設計書の変換
    1. 新規開発の実装
    1. 追加開発の実装
    1. トラブルシューティングTips
1. テスト
    1. 画面テスト
    1. 機能間結合テスト
1. まとめ




### 全体ワークフロー図
[開発ワークフロー図](./開発全体のワークフロー図.md)


## 1. 環境構築

### Clineの実行環境構築

各開発者で VSCode & Cline 拡張機能をセットアップします。

### チーム開発用の環境構築

チーム開発用に以下を用意します。
プロジェクトに特に制約がなければ、GitHubを使用します。

- ソースコードレポジトリ
- CI/CD環境

ソースコードリポジトリには下記のテンプレートを設置してください

※このリポジトリからZipダウンロード or Cloneする。

**リポジトリ構成**
```
C:.
├─.clinerules        
│  └─workflows       # Cline用のコマンドmdを設定しているフォルダ
├─docs               # Clineの使用手順を示したドキュメント群
├─memory-bank        # Cline用のメモリーバンクテンプレ
├─convert2md_scripts            # マークダウンへの変換用スクリプトが入っている
│  └─設計書
├─templates          # 変換後のマークダウン用の設計書テンプレ
│  └─design
│      ├─batches
│      ├─db_tables
│      └─screen
```

## 2. md形式の設計書へ変換

設計はこれまで通り、.xlsxファイル、.docxファイル形式で作成します。
作成したものをPythonコマンドを使用して.mdファイルに変換します。

### 対象設計書
- すべての設計書をこのフォルダに入れてください。（可能な限り多くの情報をAIに渡す）

### mdへの変換手順
1. convert2md_scriptsフォルダ内に記載されたものに従って変換（[変換手順ドキュメント](../convert2md_scripts/README.md)）
2. 変換後、 clineへ *`/adjust_md_design.md`* コマンドを使用してテンプレの形の設計書に成形
3. `docs/design` 配下に成形された設計書が生成されます。


## 3. 実装

### Step0. Cline の Memory Bank の設定
Cline には Memory Bank という機能があり、Cline との会話開始時に自動で読み込むファイルを指定できます。

これはClineのための記憶の保管庫であり、人間による深いレビューは現段階では不要です。

#### 手順

1. リポジトリの構成があっているか確認してください。（環境構築の部分参照）
2. Clineに *`/init-memory-bank`* と入力し、メモリーバンクの初期化をします。（`/ini`くらいまで入力したら補完で出てきます。）
3. Clineが自動でMemory-bank内のテンプレートを更新してくれます。

### Step1. /plan を使用して実装計画を作成
cline に *`/plan`* コマンドを送信し、実装計画を作成してもらいます。（`docs/implements_plan.md` として生成されます。）

### Step2. Clineを使ってタスクごとにコーディング

流れとしては、 **`Clineがタスクリストを生成 → ユーザーが確認＆承認 → Clineがタスクリストに沿って実装を開始`** 

#### 手順
1. 作業用のブランチへ移動 or 新規作成 (main ブランチのまま作業しない。)
1. *`/tasks.md タスクID`* と Cline へ送信（例 `/tasks.md T-001` タスクIDは実装計画内に記載されています。）
1. Cline が `docs/design` 配下の設計書ファイルをもとに `_tasks/` 配下にタスクリストを作成する
    - `docs/design` 配下の設計書だけからでは、不十分な情報をTBDとして、記録されます。
        **TBDが作成された場合は下記の2パターンのプロンプトを使い、TBDの解消を狙います。**

        ①TBDに対して `converted_md_design/フォルダ` を参照させて解決↓
        ```
        `convert2md_scripts\converted_md_design`配下のファイルを参照して下記のTBDを解消して。
        ---提示されたTBDを張り付け---
        ```
        ※ `converted_md_designフォルダ` はmdへの変換手順で作成された成形前の生の設計書情報。

        ②TBDに対して人間が回答↓
        ```
        TBD-001: 〇〇の方針を採用
        TBD-002: ...
        ```
1. *`/implement.md @\_tasks/tasklist`* を送信。(`@`でタスクリストを添付して送信)
1. Clineが実装を開始。
1. 完了前にClineが止まってしまった場合は「続きのタスクを実行して」など同じ会話の中でプロンプトを送信。
1. 機能の実装が終わるまで基本的には一つの会話の中でやり取りを行う。
1. すべてのPRタスクが完了したら、ビルドコマンドとローカルでの起動コマンドを実行し、アプリケーションが正常に起動することを確認する。
1. 一つのタスクの実装が完了したら *`update memory-bank`* とプロンプトを入力し会話を終了する
1. 実装計画にあるタスクに対してこの手順を繰り返す。Clineとの会話はタスクごとに新規作成する。

エラーや思った挙動と異なる場合は下記の修正ガイドを参考に修正を依頼する

- [修正ガイド](./fix_guid.md)

### Step3. 品質チェック・テスト実行



## Tips

### 画面遷移図の取り扱い
- エクセルに図で書いているなら Copilot を使って mermaid記法でdocsに設置する
- memory-bankの中に反映させることができる

#### 手順
1. エクセルを開く
2. 画面面遷移図をキャプチャ
3. Copilot に画像を添付して下記のプロンプトを追記
4. エラーが出た場合は `正しくレンダリングできていないので修正して` とプロンプトを打つ
5. 完成したコードを `docs/画面遷移図.md` として保存

**プロンプト**
```
画像をMermaid記法で出力してほしい

```