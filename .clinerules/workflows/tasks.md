# 依頼（改善版）

下記のコマンド入力を受け取ったら、作業管理用のタスクリスト（PR単位）を作成し、指定ファイルに出力してコミットしてください。

---

## 入力形式

このコマンドは `/tasks.md [T-00]` の形式で入力されます。

* `T-00` はタスクIDです。

---

## 目的

* 作業管理用のタスクリストを作成する
* 意味のある最小単位のPR単位に分割する
* 細かくコミットを行い、進捗を管理しやすくする

---

## 参照情報（必須）

* `docs/implements_plan.md`：タスクIDに対応する実装対象・概要の確認
* `docs/` ディレクトリ配下の設計書：実装方針・仕様・I/F・制約の確認

---

## 重要なルール（必須）

### 0) 【追加】前提条件・受け入れ基準は「ターミナルのコマンドで検証可能」にする

* **前提条件**と**受け入れ基準**は、原則として「コマンド + 期待結果（終了コード/出力/差分）」の形で書く。
* 1チェック = 1コマンドで完了する粒度を目安にする（複雑なら分割）。
* **期待結果は観測可能**にする（例：`exit code 0`、特定文字列が含まれる/含まれない、該当ファイルが存在、テストが通る、など）。
* 使うコマンドは**リポジトリ内で一般的に利用できるもの**に限定する（例：`git`, `ls`, `test`, `cat`, `sed`, `awk`, `grep`, `rg`, `jq`, `make`, 言語ランタイムの標準ツール）。
* 特定ツールの存在が不明な場合は、**まず存在確認コマンド**を前提条件にする：

  * 例：`command -v rg >/dev/null` / 期待：exit code 0
* 仕様・設計上の不足により受け入れ基準が確定できない場合は `TBD` とし、**不足情報を埋めるための確認コマンド（検索/参照）**を前提条件に落とす。

> 推奨フォーマット（前提条件/受け入れ基準の各チェック項目）：
>
> * [ ] 確認: `<command>` / 期待: `<expected>`

---

### 1) 仕様の補完禁止・不足時の扱い

* **設計書・計画書に書かれていない仕様は推測で補完しない。**
* `docs/implements_plan.md` に **タスクIDが存在しない場合**：

  * **`_tasks/<タスクID>.md` は作成しない**
  * 「見つからない旨」と「追加で必要な情報（質問項目）」を箇条書きで出力して終了する
* `docs/` に必要な設計情報が不足している場合：

  * 不足点は **`TBD`** としてテンプレに記載し、
  * **不足情報を「前提条件」のチェックリストに落とす**（後で埋められる形にする）
  * このとき、前提条件は**検索/参照で確認できるコマンド**を付ける（例：`rg -n "<キーワード>" docs/` など）

---

### 2) PR分割ルール（最小で意味のある単位）

* **1PR = 1目的**（機能追加/リファクタ/テスト/ドキュメント/インフラを混在させない）
* PR同士に依存関係がある場合は、後続PRの「前提条件」に **`PR-XXX` を明記**し、可能なら**確認コマンド**も書く（例：該当ブランチ/タグ/コミットがある、など）
* PRは **`PR-001` から連番**で採番し、**最大 `PR-009`** まで（超える場合は統合して調整）
* 受け入れ基準は **検証可能**（確認手順・観測可能な結果・テスト条件など）な形で書く

---

### 3) コミット要件

* コミットメッセージ形式：`[<タスクID>][PR-XXX] <要約>`
* `_tasks/<タスクID>.md` の作成・更新は **単独コミット**にする
* 実装コミットは **ビルド/テストが破綻しない単位**で刻む（中間状態で壊れたままにしない）

---

### 4) 出力仕様（厳守）

* 出力先：`_tasks/<タスクID>.md`（このファイルのみ作成/更新）
* テンプレのセクションは **省略しない**
* 「変更予定ファイル」は **必ずパス**で書く（不明なら `TBD`）
* 「更新日」は **実行当日の日付（YYYYMMDD）** を入れる（例：`date +%Y%m%d`）
* 「バージョン」は `1.0.0` から開始し、追記更新時のみ適切に更新する

---

## 手順

1. `docs/implements_plan.md` からタスクID `T-00` のタスクを確認する

   * 存在しない場合は「重要なルール（必須）」に従い終了する
2. `docs/` 配下の設計書から、実装に必要な仕様・制約・I/F・データ構造・受け入れ条件を抽出する
3. PR分割ルールに従い、タスクを **PR単位**に分割する

   * 依存関係は「前提条件」に `PR-XXX` として明記し、可能なら**確認コマンド**を付ける
4. 下記テンプレートに沿って、`_tasks/<タスクID>.md` を作成する
5. 日本語メッセージでコミットする（コミット要件に従う）

---

## テンプレート（厳守）

```md
## PR-001: PR単位のタスク名 [ ]
- 目的:
- 前提条件:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`
- 変更予定ファイル:
  - 新規 `...`
  - 変更 `...`
  - 削除 `...`
  - 生成物（自動生成/ビルド成果物） `...`
- 実行タスク:
  - [ ] ...
  - [ ] ...
- 受け入れ基準:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`

---

## PR-002: PR単位のタスク名 [ ]
- 目的:
- 前提条件:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`
- 変更予定ファイル:
  - 新規 `...`
  - 変更 `...`
  - 削除 `...`
  - 生成物（自動生成/ビルド成果物） `...`
- 実行タスク:
  - [ ] ...
  - [ ] ...
- 受け入れ基準:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`

---

## PR-00...: ... [ ]
- 目的:
- 前提条件:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`
- 変更予定ファイル:
  - 新規 `...`
  - 変更 `...`
  - 削除 `...`
  - 生成物（自動生成/ビルド成果物） `...`
- 実行タスク:
  - [ ] ...
  - [ ] ...
- 受け入れ基準:
  - [ ] 確認: `<command>` / 期待: `<expected>`
  - [ ] 確認: `<command>` / 期待: `<expected>`

---

更新日: YYYYMMDD
バージョン: 1.0.0
```

---

## 制限

* 設計・計画にない内容を推測で追加しない（不明点は `TBD` + 前提条件チェックリスト化）
* PR分割は最大 `PR-009` まで（超える場合は統合して調整）
* 出力は `_tasks/<タスクID>.md` のみ（他ファイルは作成しない）
* テンプレのセクションは省略しない

---

## 参考：コマンド検証の書き方例（そのままコピペ可）

* ファイルが存在する

  * 確認: `test -f path/to/file` / 期待: `exit code 0`
* ディレクトリが存在する

  * 確認: `test -d path/to/dir` / 期待: `exit code 0`
* 文字列が含まれる/含まれない

  * 確認: `rg -n "PATTERN" path/to/target` / 期待: `該当行が表示される`
  * 確認: `! rg -n "PATTERN" path/to/target` / 期待: `exit code 0（マッチなし）`
* JSONのスキーマ/キーが存在する（jqがある場合）

  * 前提: `command -v jq >/dev/null` / 期待: `exit code 0`
  * 確認: `jq -e '.key' file.json >/dev/null` / 期待: `exit code 0`
* ユニットテストが通る（プロジェクト標準コマンドが明記されている場合のみ）

  * 確認: `<project test command>` / 期待: `exit code 0`
* 変更差分が特定ファイルのみに収まっている

  * 確認: `git diff --name-only` / 期待: `_tasks/<タスクID>.md のみ`
